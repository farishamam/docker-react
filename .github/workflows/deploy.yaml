name: Deploy Frontend
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: docker build -t farishamam/docker-react -f Dockerfile.dev .
      - run: docker run -e CI=true farishamam/docker-react npm run test